// CAPSULE 2 — HPR REGISTRY v1.0.1 (P11 Phase 3, 2026-09-12).
// A SECOND, DIFFERENT application: the portable-runtime program record.
// v1.0.1: record bodies use clean em-dash text (v1.0.0 displayed literal entities).
// Same sealed engine bytes as capsule v8; different state, different UI, same runtime.
// All 64-hex strings are PUBLIC chain/manifest hashes, publicly verifiable.
export const CAPSULE_REGISTRY = {"manifest":{"name":"HPR Registry","version":"1.0.1","contract":"walkout-contract v0.7 (registry capsule, sealed read-only)","program":"P11 Phase 3 — the second capsule","origin":{"substrate":"witness-sandbox","built":"2026-09-12T19:10:00Z","note":"v1.0.1 — record bodies rebuilt with clean em-dash text (v1.0.0 displayed literal &mdash; entities); real program record, not test state"}},"ui_manifest":{"/":"c91b3463091fe563d71945f912f77f8f3b3c0dd0498522f930a35db11e38e3e9","/app.css":"8616cedce4778973d863d5e081f5119c162cbb184399fc83b53f42508d7aa9fc","/app.js":"565bd72ebdcc34caa77c623d46bb64b7befe7aba1316d45c9e6fc2f7872869b3","/icon.svg":"85a8fcea9d668dc33df46cb6fde69108357108e95488f1bd2f5cd5730a775fb4","/manifest.json":"ced2b061f8615af36d9e03160ba8ca60cd32191f8a9043d7ccf94a12b6c112e1","/sw.js":"5be88993b28d80c4aa8b1a1a13682a9ae10294229891e4d6448d7ab6b6c85466"},"ui":{"/":"<!doctype html><html lang=\"en\"><head><meta charset=\"utf-8\"><meta name=\"viewport\" content=\"width=device-width,initial-scale=1\"><title>HPR Registry</title><meta name=\"theme-color\" content=\"#0a7d3c\"><link rel=\"icon\" href=\"icon.svg\"><link rel=\"manifest\" href=\"manifest.json\"><link rel=\"stylesheet\" href=\"app.css\"></head><body><header><div><b>HARZ Runtime Registry</b><div class=\"v\">capsule 2 &middot; served by the same unmodified HPR core &middot; 2026-09-12</div></div></header><div class=\"wrap\"><div class=\"card\"><h2>State</h2><div id=\"st\">loading&hellip;</div></div><div class=\"card\"><h2>Program record</h2><div id=\"recs\">loading&hellip;</div></div><div class=\"card\"><h2>What this is</h2><p class=\"mut\">This is a SECOND, different HARZ application &mdash; a registry of the portable-runtime research program &mdash; served by the SAME sealed-engine runtime that serves the P10 book at the root of this deployment. One runtime, many capsules. Writes here are sealed shut: there is no walkout marker in this capsule&rsquo;s chain.</p></div><footer>HPR Registry &middot; experiment, not a product &middot; HARZ Digital Services</footer></div><script src=\"app.js\" defer></script></body></html>","/app.css":"*{margin:0;padding:0;box-sizing:border-box}body{font-family:-apple-system,system-ui,sans-serif;background:#f0f2f5;color:#1a1a1a}header{background:#0a7d3c;color:#fff;padding:14px 16px}header b{font-size:17px}header .v{font-size:11px;opacity:.85}.wrap{max-width:760px;margin:0 auto;padding:16px}.card{background:#fff;border-radius:12px;padding:14px 16px;margin:12px 0;box-shadow:0 1px 4px rgba(0,0,0,.08)}h2{font-size:14px;color:#0a7d3c;margin-bottom:8px}.ok{color:#0a7d3c;font-weight:600}.mut{color:#666;font-size:12px;line-height:1.5}.rec{padding:6px 0;border-bottom:1px solid #eee;font-size:13px}footer{text-align:center;color:#777;font-size:11px;padding:16px 12px 32px}","/app.js":"const V=s=>s.replace(/&/g,\"&amp;\").replace(/</g,\"&lt;\").replace(/>/g,\"&gt;\");\nasync function load(){const v=await(await fetch(\"api/verify\")).json();document.getElementById(\"st\").innerHTML='<span class=\"ok\">'+V(v.verdict)+'</span><br><span class=\"mut\">records: '+v.records+\" &middot; seals: \"+v.chain_length+\" &middot; digest: \"+String(v.digest).slice(0,24)+\"&hellip; &middot; ui: \"+String(v.ui_ok?\"SEALED\":\"?\")+\" &middot; code: \"+String(v.code_ok?\"SEALED\":\"?\")+'</span>';const r=await(await fetch(\"api/records\")).json();document.getElementById(\"recs\").innerHTML=(r.records||[]).map(x=>'<div class=\"rec\"><b>'+x.id+\"</b> &middot; \"+V(x.body)+\"</div>\").join(\"\")}\nif(\"serviceWorker\" in navigator)navigator.serviceWorker.register(\"sw.js\");\nload();","/icon.svg":"<svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"0 0 100 100\"><rect width=\"100\" height=\"100\" rx=\"20\" fill=\"#0a7d3c\"/><path d=\"M20 55h60M50 25v40\" stroke=\"#fff\" stroke-width=\"8\" fill=\"none\" stroke-linecap=\"round\"/></svg>","/manifest.json":"{\"name\":\"HPR Registry\",\"short_name\":\"Registry\",\"start_url\":\"./\",\"scope\":\"./\",\"display\":\"standalone\",\"background_color\":\"#f0f2f5\",\"theme_color\":\"#0a7d3c\",\"icons\":[{\"src\":\"icon.svg\",\"sizes\":\"any\",\"type\":\"image/svg+xml\"}]}","/sw.js":"self.addEventListener(\"install\",e=>self.skipWaiting());self.addEventListener(\"activate\",e=>self.clients.claim());self.addEventListener(\"fetch\",e=>{});"},"code_manifest":{"/engine.js":"f69339b6f08a8b17ebe38870ed2ca2ec491e4ba5b6cca2ced0b247550860ecb2"},"code":{"/engine.js":"const CONTRACT = 'walkout-contract v0.7 — state + UI + code + writes + MULTI-MIRROR ANCHOR walk';\n\nasync function sha256hex(s) {\n  const d = await crypto.subtle.digest('SHA-256', new TextEncoder().encode(s));\n  return [...new Uint8Array(d)].map(b => b.toString(16).padStart(2, '0')).join('');\n}\n\nasync function linesOf(m) {\n  return Object.keys(m).sort().map(p => p + '|' + m[p]).join('\\n');\n}\n\nasync function digestOf(state, uiManifest, codeManifest) {\n  const recs = state.records.map(r => r.id + '|' + r.body + '|' + r.created).join('\\n');\n  const ch = state.chain.map(r => r.id + '|' + r.kind + '|' + r.ref + '|' + r.payload + '|' + r.prev_hash + '|' + r.hash).join('\\n');\n  return await sha256hex('RECORDS\\n' + recs + '\\nCHAIN\\n' + ch + '\\nUI\\n' + await linesOf(uiManifest) + '\\nCODE\\n' + await linesOf(codeManifest));\n}\n\nasync function verifyChain(chain) {\n  let expected = 'GENESIS', issues = [];\n  for (const r of chain) {\n    if (r.prev_hash !== expected) issues.push('chain break at ' + r.id);\n    const h = await sha256hex(r.prev_hash + '|' + r.kind + '|' + r.ref + '|' + r.payload);\n    if (h !== r.hash) issues.push('hash mismatch at ' + r.id);\n    expected = r.hash;\n  }\n  return { ok: issues.length === 0, issues };\n}\n\nasync function hashAssets(files) {\n  const m = {};\n  for (const p of Object.keys(files).sort()) m[p] = await sha256hex(files[p]);\n  return m;\n}\n\nfunction activeMarker(chain) {\n  let marker = null;\n  for (const r of chain) {\n    if (r.kind === 'walkout_marker') marker = r;\n    else if (r.kind === 'return') marker = null;\n  }\n  return marker;\n}\n\nasync function canWrite(state) {\n  const marker = activeMarker(state.chain || []);\n  if (!marker) return { ok: false, reason: 'WRITES SEALED — no active walkout marker (v0.6)' };\n  return { ok: true, mode: 'walkout', marker: marker.hash };\n}\n\nasync function appendWithSeal(state, uiManifest, codeManifest, body, created, actor) {\n  const cw = await canWrite(state);\n  if (!cw.ok) return { ok: false, reason: cw.reason };\n  if (!body || !created) return { ok: false, reason: 'body and created required' };\n  const lastRec = state.records.length ? state.records[state.records.length - 1] : { id: 0 };\n  const id = (Number(lastRec.id) || 0) + 1;\n  const rec = { id, body: String(body).slice(0, 300), created: String(created) };\n  const tip = state.chain.length ? state.chain[state.chain.length - 1].hash : 'GENESIS';\n  const payload = JSON.stringify({ id, body: rec.body, created: rec.created, actor: String(actor || 'stranger') });\n  const hash = await sha256hex(tip + '|record|' + id + '|' + payload);\n  const seal = { id: state.chain.length + 1, kind: 'record', ref: String(id), payload, prev_hash: tip, hash };\n  const newState = { records: state.records.concat([rec]), chain: state.chain.concat([seal]) };\n  return { ok: true, record: rec, seal, state: newState, digest: await digestOf(newState, uiManifest, codeManifest) };\n}\n\nasync function ingestReturn(sourceState, uiManifest, codeManifest, survivorExport) {\n  const fail = (verdict) => ({ ok: false, verdict });\n  const ANCHOR_URLS = ['https://raw.githubusercontent.com/rabiuhamza11/harz-survivor/main/anchors/ledger.md', 'https://cdn.jsdelivr.net/gh/rabiuhamza11/harz-survivor@main/anchors/ledger.md', 'https://raw.githack.com/rabiuhamza11/harz-survivor/main/anchors/ledger.md'];\n  if (!survivorExport || !survivorExport.state) return fail('BROKEN — malformed survivor export');\n  const sChain = survivorExport.state.chain || [];\n  const marker = activeMarker(sChain);\n  if (!marker) return fail('BROKEN — no active walkout marker in survivor export');\n  const srcChain = sourceState.chain || [];\n  let mi = -1;\n  for (let i = 0; i < srcChain.length; i++) if (srcChain[i].hash === marker.hash) { mi = i; break; }\n  if (mi === -1) return fail('BROKEN — marker mismatch: not my walkout');\n  const postMarker = srcChain.slice(mi + 1);\n  const bookSeals = postMarker.filter(s => s.kind !== 'code_pin' && s.kind !== 'ui_manifest');\n  if (bookSeals.length) return fail('FORK DETECTED — source has post-marker book seals');\n  const baseRecords = sourceState.records || [];\n  const allRecords = survivorExport.state.records || [];\n  if (allRecords.length < baseRecords.length) return fail('BROKEN — base records missing');\n  for (let i = 0; i < baseRecords.length; i++) {\n    const a = baseRecords[i], b = allRecords[i];\n    if (String(a.id) !== String(b.id) || a.body !== b.body || a.created !== b.created)\n      return fail('BROKEN — base records rewritten');\n  }\n  let mIdx = -1;\n  for (let i = 0; i < sChain.length; i++) if (sChain[i].hash === marker.hash) { mIdx = i; break; }\n  const overlay = sChain.slice(mIdx + 1);\n  const overlayRecords = allRecords.slice(baseRecords.length);\n  const composed = srcChain.slice(0, mi + 1).concat(overlay);\n  const v = await verifyChain(composed);\n  if (!v.ok) return fail('BROKEN — overlay does not chain from the marker: ' + v.issues.join('; '));\n  const baseDigest = await digestOf(sourceState, uiManifest, codeManifest);\n  let ledger = null, servedFrom = null, anyReachable = false;\n  for (const u of ANCHOR_URLS) {\n    try {\n      const res = await fetch(u, { cache: 'no-store' });\n      if (!res.ok) continue;\n      const text = await res.text();\n      anyReachable = true;\n      if (text.includes(baseDigest)) { ledger = text; servedFrom = u; break; }\n    } catch (e) { continue; }\n  }\n  if (!anyReachable) return fail('BROKEN — ANCHOR UNREACHABLE — return refused fail-closed (all mirrors)');\n  if (!ledger) return fail('BROKEN — ANCHOR MISMATCH — pre-marker base ' + baseDigest.slice(0, 16) + '… not found in any reachable anchored history');\n  const anchorProof = { url: servedFrom, anchored_base: baseDigest, mirrors_tried: ANCHOR_URLS.length };\n  const tip = composed.length ? composed[composed.length - 1].hash : 'GENESIS';\n  const payload = JSON.stringify({ ingested_seals: overlay.length, ingested_records: overlayRecords.length, marker: marker.hash, anchor: anchorProof });\n  const rhash = await sha256hex(tip + '|return|walkout|' + payload);\n  const returnSeal = { id: composed.length + 1, kind: 'return', ref: 'walkout', payload, prev_hash: tip, hash: rhash };\n  const newState = { records: baseRecords.concat(overlayRecords), chain: composed.concat([returnSeal]) };\n  return { ok: true, verdict: 'RETURN COMPLETE — one book, one history, anchor-verified', state: newState, ingested: { seals: overlay.length, records: overlayRecords.length }, digest: await digestOf(newState, uiManifest, codeManifest) };\n}\n\nasync function verifyExport(x) {\n  const issues = [];\n  if (!x || !x.state || !x.ui_manifest || !x.ui || !x.code_manifest || !x.code || !x.digest)\n    return { ok: false, issues: ['malformed export'], verdict: 'BROKEN — malformed export' };\n  const chain = x.state.chain || [];\n  const v = await verifyChain(chain);\n  issues.push(...v.issues);\n  let seenMarker = false;\n  for (const r of chain) {\n    if (r.kind === 'walkout_marker') seenMarker = true;\n    else if (r.kind === 'return' && !seenMarker) issues.push('return seal without walkout marker at ' + r.id);\n  }\n  const uiH = await hashAssets(x.ui);\n  for (const p of Object.keys(x.ui_manifest)) {\n    if (!x.ui[p]) issues.push('missing ui asset ' + p);\n    else if (uiH[p] !== x.ui_manifest[p]) issues.push('ui hash mismatch at ' + p);\n  }\n  const codeH = await hashAssets(x.code);\n  for (const p of Object.keys(x.code_manifest)) {\n    if (!x.code[p]) issues.push('missing code asset ' + p);\n    else if (codeH[p] !== x.code_manifest[p]) issues.push('code hash mismatch at ' + p);\n  }\n  const pinnedUi = [...chain].reverse().find(s => s.kind === 'ui_manifest');\n  if (!pinnedUi) issues.push('no ui_manifest pin in chain');\n  else if (pinnedUi.payload !== JSON.stringify(x.ui_manifest)) issues.push('ui_manifest does not match its chain pin');\n  const pinnedCode = [...chain].reverse().find(s => s.kind === 'code_pin');\n  if (!pinnedCode) issues.push('no code_pin in chain');\n  else if (pinnedCode.payload !== JSON.stringify(x.code_manifest)) issues.push('code_manifest does not match its chain pin');\n  const d = await digestOf(x.state, x.ui_manifest, x.code_manifest);\n  if (d !== x.digest) issues.push('digest mismatch');\n  const ok = issues.length === 0;\n  return {\n    ok,\n    issues,\n    digest: d,\n    writes: activeMarker(chain) ? 'WALKOUT ACTIVE — sealed writes open' : 'WRITES SEALED — no active walkout marker',\n    verdict: ok\n      ? 'CAPSULE VERIFIED — chain intact, records sealed, ui sealed, code sealed, digest matches (v0.6)'\n      : 'BROKEN — ' + issues.join('; ')\n  };\n}"},"state":{"records":[{"id":1,"body":"P7 — state survives its platform: the CF source was killed dead for seven minutes; the Deno survivor held the sealed digest 6c6eb353 throughout.","created":"2026-09-12T19:10:00.000Z"},{"id":2,"body":"P8 — the code walks: the engine itself was hashed into the chain; the verdict became a function of the sealed bytes, not the operator.","created":"2026-09-12T19:10:00.000Z"},{"id":3,"body":"P9 — the writable capsule: a stranger wrote on the book while its origin was dead; the return gate ingested the seals byte-exact, one book, one history.","created":"2026-09-12T19:10:00.000Z"},{"id":4,"body":"P10 — the anchor-gated return: the homecoming gate checked public anchored history, refused forks, and closed the walkout at D6 2496033f.","created":"2026-09-12T19:10:00.000Z"},{"id":5,"body":"P11 Phase 1 — runtime extraction: the wrapper glue became a substrate-agnostic core (hpr-1.0.0); the book never moved at 077bc802.","created":"2026-09-12T19:10:00.000Z"},{"id":6,"body":"P11 Phase 2 — three-substrate parity: Cloudflare, Deno and Node.js all computed the identical digest 077bc802 from one capsule through the same core.","created":"2026-09-12T19:10:00.000Z"},{"id":7,"body":"This registry is capsule 2: a second, different app served by the SAME unmodified runtime — the answer to the multi-application question is yes.","created":"2026-09-12T19:10:00.000Z"}],"chain":[{"id":1,"kind":"ui_manifest","ref":"ui","payload":"{\"/\":\"c91b3463091fe563d71945f912f77f8f3b3c0dd0498522f930a35db11e38e3e9\",\"/app.css\":\"8616cedce4778973d863d5e081f5119c162cbb184399fc83b53f42508d7aa9fc\",\"/app.js\":\"565bd72ebdcc34caa77c623d46bb64b7befe7aba1316d45c9e6fc2f7872869b3\",\"/icon.svg\":\"85a8fcea9d668dc33df46cb6fde69108357108e95488f1bd2f5cd5730a775fb4\",\"/manifest.json\":\"ced2b061f8615af36d9e03160ba8ca60cd32191f8a9043d7ccf94a12b6c112e1\",\"/sw.js\":\"5be88993b28d80c4aa8b1a1a13682a9ae10294229891e4d6448d7ab6b6c85466\"}","prev_hash":"GENESIS","hash":"93f80c94b0fd9118460f6c3e1d2db9772b44b05f2fe3818658de796b9fb94394"},{"id":2,"kind":"code_pin","ref":"code","payload":"{\"/engine.js\":\"f69339b6f08a8b17ebe38870ed2ca2ec491e4ba5b6cca2ced0b247550860ecb2\"}","prev_hash":"93f80c94b0fd9118460f6c3e1d2db9772b44b05f2fe3818658de796b9fb94394","hash":"551b48154c0ad9142182e51705ab7f0956d1699649ff1ea09d764e82ebff5845"},{"id":3,"kind":"record","ref":"1","payload":"{\"id\":1,\"body\":\"P7 — state survives its platform: the CF source was killed dead for seven minutes; the Deno survivor held the sealed digest 6c6eb353 throughout.\",\"created\":\"2026-09-12T19:10:00.000Z\",\"actor\":\"witness-builder\"}","prev_hash":"551b48154c0ad9142182e51705ab7f0956d1699649ff1ea09d764e82ebff5845","hash":"7c8d7fb0a87aa4f8a1dd32c41d5b1d0795104d9cc5716e13b69ea36f949171ac"},{"id":4,"kind":"record","ref":"2","payload":"{\"id\":2,\"body\":\"P8 — the code walks: the engine itself was hashed into the chain; the verdict became a function of the sealed bytes, not the operator.\",\"created\":\"2026-09-12T19:10:00.000Z\",\"actor\":\"witness-builder\"}","prev_hash":"7c8d7fb0a87aa4f8a1dd32c41d5b1d0795104d9cc5716e13b69ea36f949171ac","hash":"753bdf3c25aca67385a22aad4e4395174614ba8aa4444343794e3af69837ad41"},{"id":5,"kind":"record","ref":"3","payload":"{\"id\":3,\"body\":\"P9 — the writable capsule: a stranger wrote on the book while its origin was dead; the return gate ingested the seals byte-exact, one book, one history.\",\"created\":\"2026-09-12T19:10:00.000Z\",\"actor\":\"witness-builder\"}","prev_hash":"753bdf3c25aca67385a22aad4e4395174614ba8aa4444343794e3af69837ad41","hash":"f3ee084c667c21f881391325d2d24acd8d474012b7ad3bc42b1622d7806fceb9"},{"id":6,"kind":"record","ref":"4","payload":"{\"id\":4,\"body\":\"P10 — the anchor-gated return: the homecoming gate checked public anchored history, refused forks, and closed the walkout at D6 2496033f.\",\"created\":\"2026-09-12T19:10:00.000Z\",\"actor\":\"witness-builder\"}","prev_hash":"f3ee084c667c21f881391325d2d24acd8d474012b7ad3bc42b1622d7806fceb9","hash":"b4677cac853cbdf34d73a69e26700c3172901d7e604496171171f60f108e1560"},{"id":7,"kind":"record","ref":"5","payload":"{\"id\":5,\"body\":\"P11 Phase 1 — runtime extraction: the wrapper glue became a substrate-agnostic core (hpr-1.0.0); the book never moved at 077bc802.\",\"created\":\"2026-09-12T19:10:00.000Z\",\"actor\":\"witness-builder\"}","prev_hash":"b4677cac853cbdf34d73a69e26700c3172901d7e604496171171f60f108e1560","hash":"db6da9be9ecbc697c1074ac51dfe5726e80d9290fa9202e71f52d6c477f2fa94"},{"id":8,"kind":"record","ref":"6","payload":"{\"id\":6,\"body\":\"P11 Phase 2 — three-substrate parity: Cloudflare, Deno and Node.js all computed the identical digest 077bc802 from one capsule through the same core.\",\"created\":\"2026-09-12T19:10:00.000Z\",\"actor\":\"witness-builder\"}","prev_hash":"db6da9be9ecbc697c1074ac51dfe5726e80d9290fa9202e71f52d6c477f2fa94","hash":"7fc56a68e2a1b3b6bdb66d48094fac7bed311adaaa18acf1d3d6bc738fdbe372"},{"id":9,"kind":"record","ref":"7","payload":"{\"id\":7,\"body\":\"This registry is capsule 2: a second, different app served by the SAME unmodified runtime — the answer to the multi-application question is yes.\",\"created\":\"2026-09-12T19:10:00.000Z\",\"actor\":\"witness-builder\"}","prev_hash":"7fc56a68e2a1b3b6bdb66d48094fac7bed311adaaa18acf1d3d6bc738fdbe372","hash":"d46bbe7d61ab71c5224378cf5f49ea6de9ebd93b9cad7bb7532ed6a2c241ad8e"}]}};
