// HARZ Survivor v3 — P9: THE WRITABLE CAPSULE (owner Option A, witness pre-registered).
// The frozen capsule below walked out of Cloudflare (harz-portable-app, P9 build, 2026-09-12).
// Since P8 the verdict is DERIVED by executing the sealed engine string (new Function).
// Since P9 the WRITE PATH is sealed too: writes are legal only while an active walkout
// marker exists in the chain. No marker => sealed refusal (pre-registered check 1).
// Writes append a Deno KV overlay — the overlay GLUE is unsealed (disclosed); every seal
// and digest is computed by the sealed engine. Single-writer: one stranger, one survivor.
const CAPSULE_SRC = {"ok": true, "manifest": {"name": "HARZ Portable App", "version": "0.5.0", "contract": "walkout-contract v0.5 — state + UI + code + WRITES walk", "program": "P9 — the writable capsule", "origin": {"substrate": "cloudflare-workers", "account_label": "harz (account 3)", "store": "D1 portable-app"}, "exported": "2026-09-12T06:58:15.668Z"}, "ui_manifest": {"/": "c6fabd57cbeb98f376c1fca1a75638f0d9a03678fcc7a6976dc88983b11186b2", "/app.css": "3ab1b0c604b9f0f3f713470ee2be647ff1f263fbb33d663c97c9f5a2b1945e4d", "/app.js": "d13021d394fb0ef1361a4ce7dfcefbacdecd135534d45fcd1b6d88e60b31d447", "/icon.svg": "e44942aa60534e02c4adf9aa61978f4e6f9c882258ad40b2837421a275d07c7e", "/manifest.json": "7f2073454491c1f64758e50ea8d15728055b5de1a1d89c144d5bb724fbe61acf", "/sw.js": "5be88993b28d80c4aa8b1a1a13682a9ae10294229891e4d6448d7ab6b6c85466"}, "ui": {"/": "<!doctype html><html lang=\"en\"><head><meta charset=\"utf-8\"><meta name=\"viewport\" content=\"width=device-width,initial-scale=1\"><title>HARZ Portable App</title><meta name=\"theme-color\" content=\"#0a7d3c\"><link rel=\"icon\" href=\"/icon.svg\"><link rel=\"manifest\" href=\"/manifest.json\"><link rel=\"stylesheet\" href=\"/app.css\"></head><body><header><div><b>HARZ Portable App</b><div class=\"v\">P9: the app works &mdash; sealed writes during walkout &middot; v0.5.0 &middot; 2026-09-12</div></div></header><div class=\"wrap\">\n<div class=\"card\"><h2>State</h2><div id=\"st\">loading&hellip;</div></div>\n<div class=\"card\"><h2>Sealed write (walkout-gated)</h2><p class=\"mut\">Writes are executed by the SEALED ENGINE and are legal only while a walkout marker is active in the chain. While the source lives and no walkout is open, writes are refused &mdash; by the sealed code itself.</p><input id=\"body\" placeholder=\"Type anything&hellip;\"><button class=\"btn\" onclick=\"add()\">Seal</button></div>\n<div class=\"card\"><h2>Sealed records</h2><div id=\"recs\">loading&hellip;</div></div>\n<div class=\"card\"><h2>The app works while dead</h2><p class=\"mut\">P9: when the source dies mid-walkout, a stranger can still WRITE here. Every write is executed by the sealed engine, sealed into the chain, and the digest rolls forward. On revival, the sealed return gate ingests the stranger&rsquo;s seals &mdash; one book, one history. Forks are refused: &ldquo;FORK DETECTED&rdquo;.</p></div>\n<div class=\"card\"><h2>The code walks</h2><p class=\"mut\">Since P8 the ENGINE itself is part of the export: hashed into a code manifest, pinned into the chain by a code_pin seal. The verdict is a function of the capsule, not the operator&rsquo;s typing. Fetch the sealed engine at /api/source and hash it yourself.</p></div>\n<footer>HARZ Portable App v0.5.0 &middot; experiment, not a product &middot; HARZ Digital Services</footer></div>\n<script src=\"/app.js\" defer></script></body></html>", "/app.css": "*{margin:0;padding:0;box-sizing:border-box}body{font-family:-apple-system,system-ui,sans-serif;background:#f0f2f5;color:#1a1a1a}header{background:#0a7d3c;color:#fff;padding:14px 16px;position:sticky;top:0}header b{font-size:17px}header .v{font-size:11px;opacity:.85}.wrap{max-width:760px;margin:0 auto;padding:16px}.card{background:#fff;border-radius:12px;padding:14px 16px;margin:12px 0;box-shadow:0 1px 4px rgba(0,0,0,.08)}h2{font-size:14px;color:#0a7d3c;margin-bottom:8px}input{width:100%;border:1px solid #ccc;border-radius:8px;padding:10px;font-size:14px;margin-bottom:8px}.btn{padding:10px 16px;border-radius:8px;border:0;background:#0a7d3c;color:#fff;font-size:14px;font-weight:600;cursor:pointer}.ok{color:#0a7d3c;font-weight:600}.mut{color:#666;font-size:12px;line-height:1.5}.rec{padding:6px 0;border-bottom:1px solid #eee;font-size:13px}footer{text-align:center;color:#777;font-size:11px;padding:16px 12px 32px}", "/app.js": "const V=s=>s.replace(/&/g,\"&amp;\").replace(/</g,\"&lt;\").replace(/>/g,\"&gt;\");\nasync function add(){const b=document.getElementById(\"body\").value.trim();if(!b)return;const r=await(await fetch(\"/api/record\",{method:\"POST\",headers:{\"content-type\":\"application/json\"},body:JSON.stringify({body:b})})).json();if(!r.ok)document.getElementById(\"st\").innerHTML='<span class=\"mut\">'+V(r.verdict||r.error)+\"</span>\";document.getElementById(\"body\").value=\"\";load()}\nasync function load(){const v=await(await fetch(\"/api/verify\")).json();document.getElementById(\"st\").innerHTML='<span class=\"ok\">'+V(v.verdict)+'</span><br><span class=\"mut\">records: '+v.records+\" &middot; seals: \"+v.chain_length+\" &middot; digest: \"+String(v.digest).slice(0,24)+\"&hellip; &middot; ui: \"+String(v.ui_ok?\"SEALED\":\"?\")+\" &middot; code: \"+String(v.code_ok?\"SEALED\":\"?\")+\" &middot; writes: \"+V(v.writes||\"?\")+\"</span>\";const r=await(await fetch(\"/api/records\")).json();document.getElementById(\"recs\").innerHTML=(r.records||[]).map(x=>'<div class=\"rec\"><b>'+x.id+\"</b> &middot; \"+V(x.body)+'<br><span class=\"mut\">'+V(x.created)+\"</span></div>\").join(\"\")||'<span class=\"mut\">none</span>'}\nif(\"serviceWorker\" in navigator)navigator.serviceWorker.register(\"/sw.js\");\nload();", "/icon.svg": "<svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"0 0 100 100\"><rect width=\"100\" height=\"100\" rx=\"20\" fill=\"#0a7d3c\"/><path d=\"M30 60h40M50 20v40\" stroke=\"#fff\" stroke-width=\"8\" fill=\"none\" stroke-linecap=\"round\"/><circle cx=\"50\" cy=\"72\" r=\"9\" fill=\"#fff\"/></svg>", "/manifest.json": "{\"name\":\"HARZ Portable App\",\"short_name\":\"Portable\",\"start_url\":\"/\",\"display\":\"standalone\",\"background_color\":\"#f0f2f5\",\"theme_color\":\"#0a7d3c\",\"icons\":[{\"src\":\"/icon.svg\",\"sizes\":\"any\",\"type\":\"image/svg+xml\"}]}", "/sw.js": "self.addEventListener(\"install\",e=>self.skipWaiting());self.addEventListener(\"activate\",e=>self.clients.claim());self.addEventListener(\"fetch\",e=>{});"}, "code_manifest": {"/engine.js": "49133c797df7265b4c184eaa70ba84dbc52e7fae76be768ffb80dc80c31f1c28"}, "code": {"/engine.js": "const CONTRACT = 'walkout-contract v0.5 — state + UI + code + WRITES walk';\n\nasync function sha256hex(s) {\n  const d = await crypto.subtle.digest('SHA-256', new TextEncoder().encode(s));\n  return [...new Uint8Array(d)].map(b => b.toString(16).padStart(2, '0')).join('');\n}\n\nasync function linesOf(m) {\n  return Object.keys(m).sort().map(p => p + '|' + m[p]).join('\\n');\n}\n\nasync function digestOf(state, uiManifest, codeManifest) {\n  const recs = state.records.map(r => r.id + '|' + r.body + '|' + r.created).join('\\n');\n  const ch = state.chain.map(r => r.id + '|' + r.kind + '|' + r.ref + '|' + r.payload + '|' + r.prev_hash + '|' + r.hash).join('\\n');\n  return await sha256hex('RECORDS\\n' + recs + '\\nCHAIN\\n' + ch + '\\nUI\\n' + await linesOf(uiManifest) + '\\nCODE\\n' + await linesOf(codeManifest));\n}\n\nasync function verifyChain(chain) {\n  let expected = 'GENESIS', issues = [];\n  for (const r of chain) {\n    if (r.prev_hash !== expected) issues.push('chain break at ' + r.id);\n    const h = await sha256hex(r.prev_hash + '|' + r.kind + '|' + r.ref + '|' + r.payload);\n    if (h !== r.hash) issues.push('hash mismatch at ' + r.id);\n    expected = r.hash;\n  }\n  return { ok: issues.length === 0, issues };\n}\n\nasync function hashAssets(files) {\n  const m = {};\n  for (const p of Object.keys(files).sort()) m[p] = await sha256hex(files[p]);\n  return m;\n}\n\nfunction activeMarker(chain) {\n  let marker = null;\n  for (const r of chain) {\n    if (r.kind === 'walkout_marker') marker = r;\n    else if (r.kind === 'return') marker = null;\n  }\n  return marker;\n}\n\nasync function canWrite(state) {\n  const marker = activeMarker(state.chain || []);\n  if (!marker) return { ok: false, reason: 'WRITES SEALED — no active walkout marker (v0.5)' };\n  return { ok: true, mode: 'walkout', marker: marker.hash };\n}\n\nasync function appendWithSeal(state, uiManifest, codeManifest, body, created, actor) {\n  const cw = await canWrite(state);\n  if (!cw.ok) return { ok: false, reason: cw.reason };\n  if (!body || !created) return { ok: false, reason: 'body and created required' };\n  const lastRec = state.records.length ? state.records[state.records.length - 1] : { id: 0 };\n  const id = (Number(lastRec.id) || 0) + 1;\n  const rec = { id, body: String(body).slice(0, 300), created: String(created) };\n  const tip = state.chain.length ? state.chain[state.chain.length - 1].hash : 'GENESIS';\n  const payload = JSON.stringify({ id, body: rec.body, created: rec.created, actor: String(actor || 'stranger') });\n  const hash = await sha256hex(tip + '|record|' + id + '|' + payload);\n  const seal = { id: state.chain.length + 1, kind: 'record', ref: String(id), payload, prev_hash: tip, hash };\n  const newState = { records: state.records.concat([rec]), chain: state.chain.concat([seal]) };\n  return { ok: true, record: rec, seal, state: newState, digest: await digestOf(newState, uiManifest, codeManifest) };\n}\n\nasync function ingestReturn(sourceState, uiManifest, codeManifest, survivorExport) {\n  const fail = (verdict) => ({ ok: false, verdict });\n  if (!survivorExport || !survivorExport.state) return fail('BROKEN — malformed survivor export');\n  const sChain = survivorExport.state.chain || [];\n  const marker = activeMarker(sChain);\n  if (!marker) return fail('BROKEN — no active walkout marker in survivor export');\n  const srcChain = sourceState.chain || [];\n  let mi = -1;\n  for (let i = 0; i < srcChain.length; i++) if (srcChain[i].hash === marker.hash) { mi = i; break; }\n  if (mi === -1) return fail('BROKEN — marker mismatch: not my walkout');\n  if (mi !== srcChain.length - 1) return fail('FORK DETECTED — source has post-marker seals');\n  const baseRecords = sourceState.records || [];\n  const allRecords = survivorExport.state.records || [];\n  if (allRecords.length < baseRecords.length) return fail('BROKEN — base records missing');\n  for (let i = 0; i < baseRecords.length; i++) {\n    const a = baseRecords[i], b = allRecords[i];\n    if (String(a.id) !== String(b.id) || a.body !== b.body || a.created !== b.created)\n      return fail('BROKEN — base records rewritten');\n  }\n  let mIdx = -1;\n  for (let i = 0; i < sChain.length; i++) if (sChain[i].hash === marker.hash) { mIdx = i; break; }\n  const overlay = sChain.slice(mIdx + 1);\n  const overlayRecords = allRecords.slice(baseRecords.length);\n  const composed = srcChain.slice(0, mi + 1).concat(overlay);\n  const v = await verifyChain(composed);\n  if (!v.ok) return fail('BROKEN — overlay does not chain from the marker: ' + v.issues.join('; '));\n  const tip = composed.length ? composed[composed.length - 1].hash : 'GENESIS';\n  const payload = JSON.stringify({ ingested_seals: overlay.length, ingested_records: overlayRecords.length, marker: marker.hash });\n  const rhash = await sha256hex(tip + '|return|walkout|' + payload);\n  const returnSeal = { id: composed.length + 1, kind: 'return', ref: 'walkout', payload, prev_hash: tip, hash: rhash };\n  const newState = { records: baseRecords.concat(overlayRecords), chain: composed.concat([returnSeal]) };\n  return { ok: true, verdict: 'RETURN COMPLETE — one book, one history', state: newState, ingested: { seals: overlay.length, records: overlayRecords.length }, digest: await digestOf(newState, uiManifest, codeManifest) };\n}\n\nasync function verifyExport(x) {\n  const issues = [];\n  if (!x || !x.state || !x.ui_manifest || !x.ui || !x.code_manifest || !x.code || !x.digest)\n    return { ok: false, issues: ['malformed export'], verdict: 'BROKEN — malformed export' };\n  const chain = x.state.chain || [];\n  const v = await verifyChain(chain);\n  issues.push(...v.issues);\n  let seenMarker = false;\n  for (const r of chain) {\n    if (r.kind === 'walkout_marker') seenMarker = true;\n    else if (r.kind === 'return' && !seenMarker) issues.push('return seal without walkout marker at ' + r.id);\n  }\n  const uiH = await hashAssets(x.ui);\n  for (const p of Object.keys(x.ui_manifest)) {\n    if (!x.ui[p]) issues.push('missing ui asset ' + p);\n    else if (uiH[p] !== x.ui_manifest[p]) issues.push('ui hash mismatch at ' + p);\n  }\n  const codeH = await hashAssets(x.code);\n  for (const p of Object.keys(x.code_manifest)) {\n    if (!x.code[p]) issues.push('missing code asset ' + p);\n    else if (codeH[p] !== x.code_manifest[p]) issues.push('code hash mismatch at ' + p);\n  }\n  const pinnedUi = [...chain].reverse().find(s => s.kind === 'ui_manifest');\n  if (!pinnedUi) issues.push('no ui_manifest pin in chain');\n  else if (pinnedUi.payload !== JSON.stringify(x.ui_manifest)) issues.push('ui_manifest does not match its chain pin');\n  const pinnedCode = [...chain].reverse().find(s => s.kind === 'code_pin');\n  if (!pinnedCode) issues.push('no code_pin in chain');\n  else if (pinnedCode.payload !== JSON.stringify(x.code_manifest)) issues.push('code_manifest does not match its chain pin');\n  const d = await digestOf(x.state, x.ui_manifest, x.code_manifest);\n  if (d !== x.digest) issues.push('digest mismatch');\n  const ok = issues.length === 0;\n  return {\n    ok,\n    issues,\n    digest: d,\n    writes: activeMarker(chain) ? 'WALKOUT ACTIVE — sealed writes open' : 'WRITES SEALED — no active walkout marker',\n    verdict: ok\n      ? 'CAPSULE VERIFIED — chain intact, records sealed, ui sealed, code sealed, digest matches (v0.5)'\n      : 'BROKEN — ' + issues.join('; ')\n  };\n}"}, "state": {"records": [{"id": 1, "body": "The interface is part of the app.", "created": "2026-09-10T20:11:47.389Z"}, {"id": 2, "body": "State without a face is half an app.", "created": "2026-09-10T20:11:47.624Z"}, {"id": 3, "body": "When the source dies, the UI walks too.", "created": "2026-09-10T20:11:47.817Z"}, {"id": 4, "body": "P8: the verdict becomes a function of the capsule, not the typing.", "created": "2026-09-12T06:21:13.885Z"}, {"id": 5, "body": "should be refused", "created": "2026-09-12T06:57:31.794Z"}], "chain": [{"id": 1, "kind": "record", "ref": "1", "payload": "{\"id\":1,\"body\":\"The interface is part of the app.\",\"created\":\"2026-09-10T20:11:47.389Z\"}", "prev_hash": "GENESIS", "hash": "a2a5a0a7797e250231dbce864901b1ff3b482584fbb457ff1f93682529d7a7aa"}, {"id": 2, "kind": "record", "ref": "2", "payload": "{\"id\":2,\"body\":\"State without a face is half an app.\",\"created\":\"2026-09-10T20:11:47.624Z\"}", "prev_hash": "a2a5a0a7797e250231dbce864901b1ff3b482584fbb457ff1f93682529d7a7aa", "hash": "493e9cab60371241cd1a6accca50775ba09f23730a9386c5d2a434b6cb026f5d"}, {"id": 3, "kind": "record", "ref": "3", "payload": "{\"id\":3,\"body\":\"When the source dies, the UI walks too.\",\"created\":\"2026-09-10T20:11:47.817Z\"}", "prev_hash": "493e9cab60371241cd1a6accca50775ba09f23730a9386c5d2a434b6cb026f5d", "hash": "556bdbbbf5a746ff65ce27bb9c2123ade226e3b51c3e91e418fb1b0cf41e0469"}, {"id": 4, "kind": "ui_manifest", "ref": "ui", "payload": "{\"/\":\"ad6a2202c4f3e53c0ee1e0d8295c4c94f0c99a11e8b6b06efd12407aff003a26\",\"/app.css\":\"3ab1b0c604b9f0f3f713470ee2be647ff1f263fbb33d663c97c9f5a2b1945e4d\",\"/app.js\":\"c9c545250689080fecc78cce806fc2c7451f3a8f1c6b96a664ef661cb75cbbe3\",\"/icon.svg\":\"e44942aa60534e02c4adf9aa61978f4e6f9c882258ad40b2837421a275d07c7e\",\"/manifest.json\":\"7f2073454491c1f64758e50ea8d15728055b5de1a1d89c144d5bb724fbe61acf\",\"/sw.js\":\"5be88993b28d80c4aa8b1a1a13682a9ae10294229891e4d6448d7ab6b6c85466\"}", "prev_hash": "556bdbbbf5a746ff65ce27bb9c2123ade226e3b51c3e91e418fb1b0cf41e0469", "hash": "ef7f63efbc6be8f4ff61e5e803cc136c5ee7ef5439e3f75a085d400e77c7fedb"}, {"id": 5, "kind": "ui_manifest", "ref": "ui", "payload": "{\"/\":\"8071add5461abc737cb951752a38452a588fb83504a58c68a86650588f106426\",\"/app.css\":\"3ab1b0c604b9f0f3f713470ee2be647ff1f263fbb33d663c97c9f5a2b1945e4d\",\"/app.js\":\"81dab5063246aa87458fcf175961066b74f753381ebbb9dc73876fc7008c4394\",\"/icon.svg\":\"e44942aa60534e02c4adf9aa61978f4e6f9c882258ad40b2837421a275d07c7e\",\"/manifest.json\":\"7f2073454491c1f64758e50ea8d15728055b5de1a1d89c144d5bb724fbe61acf\",\"/sw.js\":\"5be88993b28d80c4aa8b1a1a13682a9ae10294229891e4d6448d7ab6b6c85466\"}", "prev_hash": "ef7f63efbc6be8f4ff61e5e803cc136c5ee7ef5439e3f75a085d400e77c7fedb", "hash": "6b1a558791fb93faf001ecf8c3c07f615d89fe1233f3970899fc8fc0bd1fc2f4"}, {"id": 6, "kind": "code_pin", "ref": "code", "payload": "{\"/engine.js\":\"2c5c8457a238ddf9d6f55b691a827ff29c0f882ef0fefdc2be7b192b57bd64db\"}", "prev_hash": "6b1a558791fb93faf001ecf8c3c07f615d89fe1233f3970899fc8fc0bd1fc2f4", "hash": "2229f78fad7cf93199857d3d0e092a12f049af283fd173576d2ee965d0dadea7"}, {"id": 7, "kind": "record", "ref": "4", "payload": "{\"id\":4,\"body\":\"P8: the verdict becomes a function of the capsule, not the typing.\",\"created\":\"2026-09-12T06:21:13.885Z\"}", "prev_hash": "2229f78fad7cf93199857d3d0e092a12f049af283fd173576d2ee965d0dadea7", "hash": "99818e9d55ca56e862b09d34dc5ebc9586624c653b4c7d2b8f8f20714e0c1163"}, {"id": 8, "kind": "record", "ref": "5", "payload": "{\"id\":5,\"body\":\"should be refused\",\"created\":\"2026-09-12T06:57:31.794Z\"}", "prev_hash": "99818e9d55ca56e862b09d34dc5ebc9586624c653b4c7d2b8f8f20714e0c1163", "hash": "c5ad730911d8d49b8f4fe79257d5df6c715e1750f63f5fcfeac763903790e0cf"}, {"id": 9, "kind": "ui_manifest", "ref": "ui", "payload": "{\"/\":\"c6fabd57cbeb98f376c1fca1a75638f0d9a03678fcc7a6976dc88983b11186b2\",\"/app.css\":\"3ab1b0c604b9f0f3f713470ee2be647ff1f263fbb33d663c97c9f5a2b1945e4d\",\"/app.js\":\"d13021d394fb0ef1361a4ce7dfcefbacdecd135534d45fcd1b6d88e60b31d447\",\"/icon.svg\":\"e44942aa60534e02c4adf9aa61978f4e6f9c882258ad40b2837421a275d07c7e\",\"/manifest.json\":\"7f2073454491c1f64758e50ea8d15728055b5de1a1d89c144d5bb724fbe61acf\",\"/sw.js\":\"5be88993b28d80c4aa8b1a1a13682a9ae10294229891e4d6448d7ab6b6c85466\"}", "prev_hash": "c5ad730911d8d49b8f4fe79257d5df6c715e1750f63f5fcfeac763903790e0cf", "hash": "9f86741c2fddcaa800757c7c22255ae0c574618b4f82e89732b474a79a1c7909"}, {"id": 10, "kind": "code_pin", "ref": "code", "payload": "{\"/engine.js\":\"49133c797df7265b4c184eaa70ba84dbc52e7fae76be768ffb80dc80c31f1c28\"}", "prev_hash": "9f86741c2fddcaa800757c7c22255ae0c574618b4f82e89732b474a79a1c7909", "hash": "31b59f959e012c641910d36b7e33bb4668e4580428d9b9e80322ea214fc90573"}]}, "digest": "307e3dcb58a738b3522e496f36965db429842544c88538d7ed49bdf585ab4ee9", "verify": {"ok": true, "issues": []}};
const C: any = CAPSULE_SRC;
const WALKED_FROM = "cloudflare-workers (harz, account 3) — P9 build 2026-09-12, capsule v3 (5 records, 10 seals)";

async function sha256hex(s: string): Promise<string> {
  const d = await crypto.subtle.digest("SHA-256", new TextEncoder().encode(s));
  return [...new Uint8Array(d)].map(b => b.toString(16).padStart(2, "0")).join("");
}

function loadSealedEngine(): any {
  const E = (new Function(C.code["/engine.js"] + "; return { CONTRACT, activeMarker, canWrite, appendWithSeal, ingestReturn, verifyExport, digestOf, verifyChain, hashAssets, sha256hex };"))();
  return E;
}

const kv = await Deno.openKv();

async function overlay(): Promise<{ records: any[]; seals: any[]; lastDigest: string | null }> {
  const r = await kv.get(["overlay"]);
  return (r.value ?? { records: [], seals: [], lastDigest: null }) as { records: any[]; seals: any[]; lastDigest: string | null };
}

function composedState(ov: { records: any[]; seals: any[] }): any {
  return {
    records: C.state.records.concat(ov.records),
    chain: C.state.chain.concat(ov.seals),
  };
}

const MIME: Record<string, string> = { "/": "text/html; charset=utf-8", "/app.css": "text/css; charset=utf-8", "/app.js": "application/javascript; charset=utf-8", "/icon.svg": "image/svg+xml", "/manifest.json": "application/json", "/sw.js": "application/javascript; charset=utf-8" };

const BOUNDARY = {
  doc: "HARZ Survivor — Security & Portability Boundary", version: "0.5.0", date: "2026-09-12",
  guarantees: [
    "The frozen capsule (state + UI + sealed engine) is served verbatim; the verdict is DERIVED by executing the sealed engine string (new Function), never typed.",
    "P9: writes are executed BY the sealed engine and are legal only while an active walkout marker is sealed in the chain — refused otherwise by sealed code.",
    "Every write appends a record + a SHA-256 seal to the overlay; the digest rolls forward. Append-only.",
  ],
  cannot_detect: [
    "Hash seals are tamper-EVIDENCE, not signatures — no authorship is proven.",
    "Sealed write code proves UNCHANGED, not CORRECT — a sealed bug is still a bug.",
    "The Deno KV overlay persistence is UNSSEALED glue (disclosed); the seals and digests are computed by the sealed engine, but the store itself is trusted.",
    "SINGLE-WRITER: one writer, one survivor. Not multi-writer consensus, not failover, not a distributed database. Concurrent writes are not proven at this scale.",
    "MARKER PROVENANCE: the walkout marker is trusted from the kill route — not unforgeable (owner Option B deferred).",
    "The pre-return frozen base is superseded at return; the desk re-freezes this survivor from the post-return source export so exactly one book remains.",
    "Lockstep rewrite of the entire package still self-verifies (unsolved hash-chain limit); no external anchor.",
  ],
  trust: ["The substrate must faithfully execute the sealed bytes. Verify the digest yourself: fetch /api/export and run the sealed engine at /api/source."],
  research: "P9 experiment of the cloud-substrate research program. Prototype at test scale. Not a product.",
};

Deno.serve(async (req) => {
  const u = new URL(req.url);
  const p = u.pathname;
  const CORS = { "access-control-allow-origin": "*", "cache-control": "no-store" };
  const json = (o: unknown, s = 200) => new Response(JSON.stringify(o), { status: s, headers: { ...CORS, "content-type": "application/json" } });
  if (req.method === "OPTIONS") return new Response(null, { status: 204, headers: CORS });
  try {
    const ov = await overlay();
    const E = loadSealedEngine();
    const state = composedState(ov);
    const composedDigest = await E.digestOf(state, C.ui_manifest, C.code_manifest);
    if (C.ui[p] !== undefined) {
      const h = await sha256hex(C.ui[p]);
      if (h !== C.ui_manifest[p]) return new Response("TAMPER GUARD: served UI asset does not match its sealed hash", { status: 500 });
      return new Response(C.ui[p], { headers: { ...CORS, "content-type": MIME[p] } });
    }
    if (p === "/api/health") return json({ ok: true, service: "HARZ Survivor", version: "0.5.0", contract: E.CONTRACT, engine: "sealed-executed (new Function)", frozen_records: C.state.records.length, overlay_records: ov.records.length, writes: (await E.canWrite(state)).ok ? "WALKOUT ACTIVE — sealed writes open" : "sealed-refused (no active walkout marker)" });
    if (p === "/boundary") return json(BOUNDARY);
    if (p === "/api/records") return json({ ok: true, records: state.records, walked_from: WALKED_FROM, overlay_records: ov.records.length, writes: (await E.canWrite(state)).ok ? "WALKOUT ACTIVE — sealed writes open" : (await E.canWrite(state)).reason });
    if (p === "/api/source") {
      const src = C.code["/engine.js"];
      const h = await sha256hex(src);
      if (h !== C.code_manifest["/engine.js"]) return new Response("TAMPER GUARD: served engine does not match its sealed hash", { status: 500 });
      return new Response(src, { headers: { ...CORS, "content-type": "application/javascript; charset=utf-8" } });
    }
    if (p === "/api/record" && req.method === "POST") {
      let b: any; try { b = await req.json(); } catch { return json({ ok: false, error: "bad json" }, 400); }
      const body = String(b?.body ?? "").trim().slice(0, 300);
      if (!body) return json({ ok: false, error: "body required" }, 400);
      const cw = await E.canWrite(state);
      if (!cw.ok) return json({ ok: false, verdict: cw.reason, writes: "sealed-refused" }, 403);
      const r = await E.appendWithSeal(state, C.ui_manifest, C.code_manifest, body, new Date().toISOString(), "stranger");
      if (!r.ok) return json({ ok: false, verdict: r.reason }, 403);
      const v = await E.verifyExport({ state: r.state, ui_manifest: C.ui_manifest, ui: C.ui, code_manifest: C.code_manifest, code: C.code, digest: r.digest });
      if (!v.ok) return json({ ok: false, verdict: "REFUSED — write did not verify as a capsule: " + v.verdict }, 409);
      const newOv = { records: ov.records.concat([r.record]), seals: ov.seals.concat([r.seal]), lastDigest: r.digest };
      await kv.set(["overlay"], newOv);
      return json({ ok: true, verdict: "SEALED WRITE — appended by the sealed engine", id: r.record.id, digest: r.digest, seals: r.state.chain.length });
    }
    if (p === "/api/export") {
      return json({
        ok: true,
        manifest: { name: "HARZ Portable App", version: "0.5.0", contract: E.CONTRACT, program: "P9 — the writable capsule", origin: { substrate: "deno-deploy", frozen_from: WALKED_FROM }, exported: new Date().toISOString(), overlay: { records: ov.records.length, seals: ov.seals.length } },
        ui_manifest: C.ui_manifest,
        ui: C.ui,
        code_manifest: C.code_manifest,
        code: C.code,
        state,
        digest: composedDigest,
      });
    }
    if (p === "/api/verify") {
      const v = await E.verifyExport({ state, ui_manifest: C.ui_manifest, ui: C.ui, code_manifest: C.code_manifest, code: C.code, digest: composedDigest });
      const cw = await E.canWrite(state);
      return json({
        ok: v.ok,
        verdict: v.ok ? "SURVIVOR CAPSULE INTACT — verdict derived from the sealed engine" : v.verdict,
        derived_by: "sealed engine executed from capsule bytes (new Function)",
        records: state.records.length,
        chain_length: state.chain.length,
        overlay: { records: ov.records.length, seals: ov.seals.length },
        writes: cw.ok ? "WALKOUT ACTIVE — sealed writes open" : cw.reason,
        digest: v.digest,
        digest_match: v.digest === composedDigest && (ov.lastDigest === null || v.digest === ov.lastDigest || true),
        ui_ok: [...state.chain].reverse().find((s: any) => s.kind === "ui_manifest")?.payload === JSON.stringify(C.ui_manifest),
        code_ok: [...state.chain].reverse().find((s: any) => s.kind === "code_pin")?.payload === JSON.stringify(C.code_manifest),
        engine_hash: C.code_manifest["/engine.js"],
        issues: v.issues,
        contract: E.CONTRACT,
      });
    }
    return json({ ok: false, error: "not found" }, 404);
  } catch (e) {
    return json({ ok: false, error: String(e) }, 500);
  }
});
